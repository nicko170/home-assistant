# UI-created automations live here. Currently empty.
#
# "Turn TV on with Apple TV" (id 1756200632989) was removed 2026-08-29 and
# replaced by packages/lounge_av.yaml. It had never worked, for three
# independent reasons:
#   - it sat DISABLED; last triggered 2025-11-17
#   - it triggered on remote.lounge_room turning on, which is a CONNECTION
#     indicator, not a power state, so it effectively only fired on HA restart
#   - it was written against opaque device_ids rather than entities
# The replacement is entity-based, forced enabled with initial_state, and
# documents each trap inline.
[]
